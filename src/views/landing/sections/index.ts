/**
 * Landing sections
 *
 * Added on top of the Split Studio base page. Each is self-contained so the
 * landing page itself only ever grows by an import and a tag — the file is
 * shared with other work in flight and should stay easy to merge.
 */

export { AppSection } from './AppSection';
export { CorridorGrid } from './CorridorGrid';
export { JourneyDetail } from './JourneyDetail';
export { ProofStrip } from './ProofStrip';
export { RateStrip } from './RateStrip';
export { TrackingBox } from './TrackingBox';
